spring:
  application:
    name: pbsetting
  cloud:
    config:
      server:
        git:
          uri: https://github.com/devkjc/pbresource
          search-paths: pbuser, pbletter, pbbird, pbpostbox
        encrypt.enabled: false

server:
  port: 5002
